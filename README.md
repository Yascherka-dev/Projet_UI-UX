
  # Upload visual assets

  This is a code bundle for Upload visual assets. The original project is available at https://www.figma.com/design/jzmZhZDoJ1Wrr0ZZ6SXAFJ/Upload-visual-assets.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # Projet_UI-UX
